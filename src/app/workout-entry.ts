export interface WorkoutEntry{
    id: number;
    name: string;
    category: string;
    advisedFor: string;
    levelOfDifficulty: string;
    equipment: string;
    sets: string;
    video: string;
}